name: WYNFLEX CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check Python syntax
        run: |
          python -m py_compile src/extract.py
          python -m py_compile src/inspect_excel.py
          python -m py_compile src/transform.py
          python -m py_compile src/load.py
          python -m py_compile src/model.py

      - name: Run tests
        run: pytest

      - name: Build Docker image
        run: docker build -t wynflex-pipeline .
